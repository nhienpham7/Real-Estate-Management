# Real Estate Management (minimal demo)

Run the dev server:

```bash
npm install
npm run dev
```

Run tests:

```bash
npm run test
```

This demo stores user and properties in `localStorage` and includes a small unit test for the booking util.
